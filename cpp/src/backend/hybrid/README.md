Hybrid backend
==============

Planned work:
- Overlap GPU triple‑kernel compute with CPU MIKX/assembly using streams and pinned transfers.
- Sliding windows in time or triple slabs to bound memory.
- Controlled via Exec{backend=Hybrid, streams, pinned}.

